import 'package:collect/collect.dart';
import 'package:flutter/material.dart';

void main() {
  AppTheme.accentError;
}

class MASG extends StatelessWidget {
  const MASG({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      theme: AppTheme.light(),
    );
  }
}
